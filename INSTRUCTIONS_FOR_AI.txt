# GenLayer: PR-to-Payout Migration Instructions

## Current Project Status
- **Contract:** `contracts/pr_to_payout.py` is fully refactored for GenVM. It uses `gl.eq_principle.strict_eq` for AI consensus and internalizes all helper functions.
- **Deployment:** The contract has been deployed to Bradbury testnet, but initial attempts failed initialization because of missing constructor arguments.
- **Fix Applied:** The browser-based deployer (`src/app/deploy/page.tsx`) has been fixed to pass the required `"github.com"` domain as an initial argument.

## Mandatory Next Steps for AI Agent
1. **Redeploy via Browser:** Navigate to `http://localhost:3000/deploy` (or the active port) and click "Deploy to Bradbury".
2. **Retrieve Address:** Extract the NEW contract address from the transaction receipt shown in the UI.
3. **Configure Environment:** Update `NEXT_PUBLIC_CONTRACT_ADDRESS` in `.env` with the new address.
4. **Test Create Bounty:** Go to the "Create Bounty" page and submit a test bounty. Verify the transaction confirms on GenLayer.
5. **Verify AI Consensus:** Submit a PR link as proof for a bounty. Check the GenLayer explorer to see the AI validators evaluating the proof and triggering the payout.
6. **Cleanup:** Once verified, delete the following temporary files before final submission:
   - `src/app/deploy/page.tsx`
   - `src/app/api/contract/route.ts`
   - `scripts/` (if any scripts were copied)
7. **Final Check:** Run `npm run build` to ensure the project is ready for production.

## Network Details
- **Network:** Bradbury Testnet
- **RPC:** https://rpc-bradbury.genlayer.com
- **Chain ID:** 4221
- **Explorer:** https://explorer-bradbury.genlayer.com

---
*Generated by Antigravity AI on 2026-04-21*
